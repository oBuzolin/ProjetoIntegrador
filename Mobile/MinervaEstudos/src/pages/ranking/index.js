import { View, StyleSheet, Text } from "react-native";

export function Ranking() {
  return (
    <View styles={styles.container}>
      <Text>Pagina Ranking</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
