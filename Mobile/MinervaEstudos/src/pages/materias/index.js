import { View, StyleSheet, Text } from "react-native";

export function Materias() {
  return (
    <View styles={styles.container}>
      <Text>Pagina Materias</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
